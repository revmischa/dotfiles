{{- /* Generated from the installed binary: `herdr --skill`. Don't hand-edit --
       the binary is the authority on its own CLI, and two things overwrite this:
       `chezmoi apply`, and a check in .zshrc that refreshes it after herdr is
       upgraded. Both write the same bytes, so they don't fight.
       .chezmoiignore drops this file on machines without herdr. */ -}}
{{ output "herdr" "--skill" | trim }}
